let name = 'Dinesh'; // String
let age = '34'; //number
let bool = true; // Boolean
let phoneNumber = undefined; // Undefinied
let addComment = null; // Null

console.log(name+' '+age+' '+bool+' '+phoneNumber+' '+addComment);